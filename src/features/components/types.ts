export type TextData = {
  color: string;
  text: string;
};

export type ImageData = {
  url: string;
  alt: string;
};

export type ButtonData = {
  text: string;
  color: string;
  backgroundColor: string;
  linkTo?: string;
};
